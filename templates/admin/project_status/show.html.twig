{% extends 'admin/dashboard.html.twig' %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_status.id }}</td>
            </tr>
            <tr>
                <th>Token</th>
                <td>{{ project_status.token }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_project_status_index') }}">back to list</a>

    <a href="{{ path('app_admin_project_status_edit', {'id': project_status.id}) }}">edit</a>

    {{ include('admin/project_status/_delete_form.html.twig') }}
{% endblock %}
