<form method="post" action="{{ path('app_admin_project_video_quality_delete', {'id': project_video_quality.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_video_quality.id) }}">
    <button class="btn">Delete</button>
</form>
